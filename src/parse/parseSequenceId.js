/**
 * @param {string} unparsed What the user input.
 * 
 * @returns {string} The parsed sequence ID.
 */
function parseSequenceId(unparsed) {
  return unparsed.trim();
}

export default parseSequenceId;
